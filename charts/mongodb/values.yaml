mongodb:
  architecture: "standalone"

  volumePermissions:
    enabled: true

  arbiter:
    enabled: false

  resources:
    requests:
      cpu: "200m"
      memory: "256Mi"
    limits:
      cpu: "400m"
      memory: "512Mi"

  nodeSelector: {}
  tolerations: []

  expose:
    enabled: true
    host: "0.0.0.0"

  service:
    # nodePort: 31827  # Uncomment and set a custom port if needed, or leave commented to let Kubernetes assign a random port

  extraFlags: "--bind_ip_all"