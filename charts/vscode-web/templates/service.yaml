apiVersion: v1
kind: Service
metadata:
  name: {{ include "vscode-web.fullname" . }}
  labels:
    {{- include "vscode-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vscode-web.selectorLabels" . | nindent 4 }}
