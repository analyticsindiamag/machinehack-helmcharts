apiVersion: v1
kind: Service
metadata:
  name: {{ include "vscode-web.fullname" . }}
  labels:
    app: {{ include "vscode-web.name" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: {{ include "vscode-web.name" . }} 