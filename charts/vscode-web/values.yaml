codeServer:
  repository: "https://github.com/kloudlite/website.git"
  password: "Feth03l67dp8Uaju0cCtMSKclmuuY" 
  branch: "release-v1.0.0"
  storage: 
    size: "5Gi"
    className: ""  # Changed from "standard" to "longhorn" which is more commonly available

  resources:
    requests:
      cpu: "200m"
      memory: "256Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"

  ingress:
    enabled: true
    className: "nginx"
    host: "vscode-web.machinehack-new.clients.kloudlite.io"  # Change this to your desired hostname
    cors:
      enabled: true
      origins: ["*"] 